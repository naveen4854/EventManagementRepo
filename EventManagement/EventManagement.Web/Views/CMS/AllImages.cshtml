@model IEnumerable<EventManagement.DataModels.ConferenceImageDTO>

@{
    ViewBag.Title = "AllImages";
    Layout = "~/Views/Shared/Layouts/_CMSLayout.cshtml";
}

<h2>AllImages</h2>

<p>
    @Html.ActionLink("Create New Image", "NewImage")
</p>

<div class="form-group">
    <div class="col-md-10">
        @Html.DropDownList("Conference", new SelectList(@ViewBag.Conferences, "Id", "Name", @ViewBag.ConfId), new { @class = "form-control", onchange = "redirect('" + Url.Action("AllImages", "CMS", new { confid = "val" }) + "')" })
    </div>
</div>
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImageUrl)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConferenceId)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConferenceId)
            </td>
            <td>
                @Html.ActionLink("Delete", "DeleteConferenceImage", new { id = item.Id })
            </td>
        </tr>
    }

</table>
<script>
    function redirect(url) {
        var val = $('#Conference').val();
        window.location = url.replace('val', val);
    }
</script>
